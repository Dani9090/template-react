import React from 'react';
import PropTypes from 'prop-types';
const UserList = () => (
  <div>
    <ul>
      <li>\<div</li>
    </ul>
  </div>
);

UserList.propTypes = {};

export default UserList;
